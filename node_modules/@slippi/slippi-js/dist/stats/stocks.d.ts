import { PlayerIndexedType, StockType } from "./common";
import { FrameEntryType, FramesType } from "../types";
import { StatComputer } from "./stats";
export declare class StockComputer implements StatComputer<StockType[]> {
    private state;
    private playerPermutations;
    private stocks;
    setPlayerPermutations(playerPermutations: PlayerIndexedType[]): void;
    processFrame(frame: FrameEntryType, allFrames: FramesType): void;
    fetch(): StockType[];
}
